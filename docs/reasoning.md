# МедЗвон — письменное рассуждение о дизайне

## Введение

МедЗвон — детерминированная система классификации намерений для русского языка, работающая на входных данных распознавания речи (STT) в телефонном звонке в медицинское учреждение. Она обязана быть **честной**: когда улики слабы или противоречивы, она возвращает строгий `UNCLEAR` (с открытой уверенностью и списком совпавших признаков), а не тихо угадывает. Пайплайн поэтапно отсекает мусор, нормализует текст, подавляет ограниченную негацию, собирает точные сигналы, затем добавляет stem- и fuzzy-улики только при необходимости и решает последним. Область выходов — шесть доменных намерений (`BOOK`, `CANCEL`, `RESCHEDULE`, `INFO`, `OPERATOR`, `COMPLAINT`) и `UNCLEAR`. Ниже — четыре темы, объясняющие ключевые решения.

Функция возвращает `{ intent, confidence, scores, matched }`. Поля `scores` и `matched` остаются в результате даже при `UNCLEAR`: следующий слой диалога видит, что именно было распознано, и может корректно переспросить.

---

## 1. Порядок этапов

Порядок пайплайна подчинён одному правилу: **отсеять дёшево, почистить один раз, убрать отрицательные улики, собрать точные сигналы, эскалировать в более неточные сопоставления только при необходимости, решать последним.**

1. **Guard первым.** Это `O(1)`-тест и требование тотальной функции: `null`/пусто/слишком короткий вход → `UNCLEAR`, никогда не бросок. Каждый цикл на нормализации мусора выброшен, а исключение из `classify` было бы багом.
2. **Normalize до tokenize.** Токенизация требует чистых границ: «приём,» нельзя разбить в токен, совпадающий с «приём», пока не убрана пунктуация, и нельзя сопоставлять без учёта регистра, пока регистр не свёрнут. Нормализуя один раз в начале, каждый следующий этап работает с канонической формой и никогда не пере-чистит.
3. **Tokenize до негации и score.** Скорер сопоставляет пред-собранные единицы. Здесь же живёт стратегия разорванных слов: STT-артефакт «за писаться» становится биграммой словаря `BOOK`, и система ловит разрывы *как фразы* — без хрупкого шага явной «склейки слов», которому понадобился бы собственный, склонный к ошибкам словарь.
4. **Негация до score.** После «не» признаки подавляются до противопоставления «а»/«но», а без него — до конца входа. Поэтому «не хочу отменять запись» не становится `CANCEL`, а «не отменить, а перенести запись» сохраняет `RESCHEDULE`. Исключение «не с роботом» остаётся положительным запросом к оператору. Это намеренно узкое правило, а не попытка полноценно разобрать русскую грамматику: в сомнительном случае безопаснее вернуть `UNCLEAR`.
5. **Точный скоринг до неточных фолбэков.** Дешёвое, детерминированное, переиспользует курируемый словарь; решает чистые кейсы (1–4) и большую часть структуры испорченных (5, 7, 8) сразу.
6. **Stem и фаззи только на фолбэке**. Stem ловит предсказуемые окончания, а Левенштейн даёт частичные улики сходства. Фаззи выбирает один лучший кандидат на токен и намерение, не пересчитывая точное совпадение; это не позволяет синонимам и повтору STT-токена искусственно завышать уверенность.
7. **Resolve** — решение взвешивает все улики.
8. **Result последним**, чтобы контракт вывода был в одном месте и нёс *причину* `UNCLEAR` вперёд — чтобы диалоговый движок мог переспросить, эскалировать или подсказать, а не получал голую метку.

## 2. Что происходит при низкой уверенности

Резолвер двух уровней: **уверенно-доменное → `UNCLEAR`.** Оба порога доменного уровня — выражения принципа «лучше переспросить, чем ввести в заблуждение» (П4):

- **Абсолютная слабость** (`confidence_top1 < minConfidence`): даже лучшее доменное намерение не поддержано сильно — ловит бессмыслицу («ыаыы ало алё» → все скоры 0) и чистые конверсационные фразы («спасибо», «до свидания»), у которых нет доменных улик вовсе.
- **Неоднозначность** (`confidence_top1 − confidence_top2 < ambiguityGap`): два доменных намерения почти сравнялись — страховочная сеть для фонетических путаниц, которые *почти* подходят под два смысла.

Система возвращает `UNCLEAR`, когда ни одно доменное намерение не проходит оба порога. На `UNCLEAR` она **не стирает** проделанную работу: возвращает уверенность лучшего отвергнутого кандидата и полные `scores`/`matched`, чтобы вызывающий диалоговый движок мог выбрать переспросить / эскалировать / предложить подсказку («вы хотели записаться или перенести?»). Открытие уверенности — а не возврат только метки — то, что позволяет принимающему решать безопасно. Система никогда тихо не угадывает, когда улики слабы или разделены; она поднимает неопределённость наверх.
